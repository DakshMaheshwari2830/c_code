#include <stdio.h>
#include <math.h>

int main() {
   int a,b,power;
   printf("Enter the number a:");
   scanf("%d",&a);
   printf("Enter the number b:");
   scanf("%d",&b);
   power=pow(a,b);
   printf("%d",power);
   
   
        
    
    return 0;
